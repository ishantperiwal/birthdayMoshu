// Temporary switch for testing with a friend: shows simple gold coins and a
// coin count instead of cash bundles and money. Nothing else changes.
// Set back to false to restore the cash bundles, € and ₹ exactly as before.
export const COIN_MODE=false;
// Wording that names the reward, for the few places that say it.
export const DASH_NAME=COIN_MODE?'coin dash':'cash dash';
